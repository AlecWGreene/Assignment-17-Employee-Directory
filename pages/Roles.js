import * as React from 'react';
import { View, Text } from 'react-native';
import RoleDataContainer from '../components/RoleDataContainer';

export default function Roles(){
    return (
        <RoleDataContainer />
    );
}